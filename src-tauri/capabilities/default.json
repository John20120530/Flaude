{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main Flaude window. Custom app commands (fs_*, shell_exec) don't need explicit permissions — they're always available to our own invoke handler. Plugin commands do need permissions, which is why dialog:default and updater:default are listed here. The updater grants only `check` + `download_and_install`; the plugin's higher-privilege commands stay opt-out.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "updater:default",
    "process:allow-restart"
  ]
}
