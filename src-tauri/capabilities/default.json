{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main Flaude window. Custom app commands (fs_*, shell_exec) don't need explicit permissions — they're always available to our own invoke handler. Plugin commands do need permissions, which is why dialog:default is listed here.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
